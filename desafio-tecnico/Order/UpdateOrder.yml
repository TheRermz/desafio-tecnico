info:
  name: UpdateOrder
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/order/va01f5689vdb"
  body:
    type: json
    data: |-
      {
        "items": [
          {
            "idItem": "p536c2b76vdb",
            "quantidadeItem": "jorge"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
