info:
  name: CreateOrder
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/order/"
  body:
    type: json
    data: |-
      {
        "items":[ {
          "idItem": "pe908e628vdb",
          "quantidadeItem": 1
        }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
