info:
  name: GetAllActiveOrders
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/order/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
