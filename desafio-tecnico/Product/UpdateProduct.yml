info:
  name: UpdateProduct
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/product/pd555f80avdb"
  body:
    type: json
    data: |-
      {
        "productName": "Igor 3k",
        "price": 12.22
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
