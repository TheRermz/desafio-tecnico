info:
  name: DeleteProduct
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/product/pd555f80avdb"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
