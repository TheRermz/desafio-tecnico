info:
  name: Create Product
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/product"
  body:
    type: json
    data: |-
      {
        "nomeProduto": "Bolos",
        "valorItem": 32.54
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
