info:
  name: GetProductById
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/product/pd555f80avdb"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
